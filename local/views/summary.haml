.summary
  - haml_tag heading do
    %a{ :href => article.abspath }= article.heading
  %img{ :src => "/attachments/#{article.thumbnail}", :alt => "Bookmark on delicous.com" }
  - if article.summary.nil? || article.summary.empty?
    :maruku
      #{article.body}
  - else
    :maruku
      #{article.summary}
    %p.read_more
      %a{ :href => article.abspath }= article.read_more
